SEEDS: 3
GENERATIONS: 30
LESSON_INTERVAL: 5
API_MAX_RETRIES: 2  # Number of retries for LLM generate
API_TIMEOUT: 100     # Timeout (seconds) for each LLM generate call

defaults:
  - override hydra/job_logging: custom
