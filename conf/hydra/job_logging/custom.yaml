version: 1
formatters:
  simple:
    format: '%(asctime)s - %(levelname)s - %(message)s'
handlers:
  console:
    class: rich.logging.RichHandler
    formatter: simple
    level: INFO
root:
  handlers: [console]

disable_existing_loggers: false
